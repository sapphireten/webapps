QUICKSTART:

This folder contains all the requirements for HW4. Included in this folder is the program files which include HTML/CSS/JavaScript. Also included is the 
flowchart. To get started, clone the repository to your local machine. Next, click on debug to run the webpage, it should load you on the homepage or otherwise known as the introductions page.
You can then interact with the webpage such as changing theme, or navigating to different tabs. 
On the vampire test page, you can input your classmates's information to see if they're a vampire either by random or threshold. 
You can then view the results on the final page or results page. 
